//-----------------------------------------------------------------------------
#ifndef CGLoggerH
#define CGLoggerH
//-----------------------------------------------------------------------------
#include "MiscTypes.h"
//-----------------------------------------------------------------------------
class CGLogger
{
public:
	CGLogger(uint _uiBufferSize = 65536);
	void Write(char *fmt,...);
	char* szGetBuffer(){ return(m_pucBuffer); };
	void Clear();

private:
	bool	m_bValid;
	// CGDynArray<char*> m_Lines;
	char* m_pucBuffer;
	uint  m_uiBufferSize;
};

//-----------------------------------------------------------------------------
#endif //-----------------------------------------------------------------------------
//-----------------------------------------------------------------------------
